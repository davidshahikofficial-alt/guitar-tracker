import React from "react";
import ReactDOM from "react-dom/client";
import GuitarTracker from "./GuitarTracker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GuitarTracker />
  </React.StrictMode>
);
